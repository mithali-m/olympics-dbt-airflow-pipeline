name: 'olympic_dbt'
version: '1.0.0'
config-version: 2

profile: 'olympic_dbt'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  olympic_dbt:
    staging:
      +materialized: table
      +schema: STAGING
    marts:
      +materialized: table
      +schema: ANALYTICS